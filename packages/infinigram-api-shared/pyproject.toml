[project]
name = "infinigram-api-shared"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opentelemetry-api>=1.41.1",
    "opentelemetry-sdk>=1.41.1",
    "saq[redis]>=0.26.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
