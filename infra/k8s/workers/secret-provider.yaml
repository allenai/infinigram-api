apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: worker-secret-provider
  namespace: infinigram-api
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/ai2-skiff2-playground/secrets/global-infinigram-api-ATTRIBUTION_QUEUE_URL/versions/latest"
        path: "attribution_queue_url"
